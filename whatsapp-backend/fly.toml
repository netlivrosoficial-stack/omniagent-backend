app = "whatsapp-bp-backend-silent-mountain-8291"
primary_region = "gru"

[build]
  builder = "docker"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1
  processes = ["app"]